const circleRadius = 5;

const circleArea = Math.PI * Math.pow(circleRadius, 2);

const circumference = 2 * Math.PI * circleRadius;

// console.log("For a circle with a radius of " + circleRadius + " units: " + " - The area is approximately " + circleArea + " square units." + " - The circumference is approximately " + circumference + " units."); //
// with new lines -> (\n) <- //
console.log(
  "For a circle with a radius of " +
    circleRadius +
    " units:\n" +
    " - The area is approximately " +
    circleArea +
    " square units.\n" +
    " - The circumference is approximately " +
    circumference +
    " units."
);
